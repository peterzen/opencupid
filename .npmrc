# .npmrc

# Enable postinstall scripts (e.g., for prisma, bcrypt)
ignore-scripts=false

# Optional: Enable strict lockfile and reproducible builds
strict-peer-dependencies=true

# Optional: Speed up install by avoiding audits
audit=false
fund=false
